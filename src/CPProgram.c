#include <stdio.h>
int main() {
    int i = 10;
    int j = 20;
    if ( i < j ){
        printf(" %d is less than %d\n", i, j);
    } else {
        printf(" %d is not less than %d\n", i, j);
    }
    return 0;
}